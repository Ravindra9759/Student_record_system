# Student Records CLI Program

A command-line interface (CLI) application to manage student records.  
Supports adding, viewing, updating, and deleting records with persistent storage in **JSON** or **CSV** formats.

---

## 🚀 Features
- **Add new student records** with validated input (roll, name,Father_name,College_name,Course,Started_date,End_date,Address,Mobile_Number,etc.)
- **View records** in a clean tabular format
- **Update existing records** by student Roll
- **Delete records** safely with confirmation
- **Persistent storage** using JSON or CSV files
- **Cross-platform CLI** built with Python

---

## 🛠 Tech Stack
- **Python 3.x**
- **JSON / CSV** for storage
- **tabulate** library for table display

---

## 📂 Project Structure

